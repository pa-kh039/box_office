import styled from 'styled-components';

import { SearchCard } from '../styled';  
// we will extend styles from searchcard

export const StyledActorCard = styled(SearchCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;




